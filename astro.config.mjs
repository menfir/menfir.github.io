// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Set this to the custom domain once DNS is pointed at GitHub Pages.
  // Used for canonical URLs; the build works without it.
  site: 'https://example.com',
});
